{
  "name": "kinetic-nanoclaw",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "python3 -m http.server 4173 --bind 127.0.0.1",
    "check": "node --check animation.js",
    "render": "node scripts/render.cjs"
  },
  "devDependencies": {
    "@napi-rs/canvas": "^1.0.9"
  }
}
